# Journal — 2026-06-18

Lot « règles tableau v2 » suite à synchro avec l'associé électricien. Brainstorm
→ spec → plan → exécution inline (12 tâches TDD), branche `feat/placement-chambre`.

## Décomposition
- **Spec A** (livrée ici) : règles tableau v2.
- **Spec B** (à venir) : détection géométrique « garage attenant » (adjacence
  polygones/murs) qui alimentera l'attribut `attenant`.

Spec : [docs/superpowers/specs/2026-06-18-regles-tableau-v2-design.md](../superpowers/specs/2026-06-18-regles-tableau-v2-design.md) ·
Plan : [docs/superpowers/plans/2026-06-18-regles-tableau-v2.md](../superpowers/plans/2026-06-18-regles-tableau-v2.md).

## Changements (6 modifs associé + extras)
1. **Différentiels A / F / AC** ([nfc_tableau.py](../../src/planrec/nfc_tableau.py)) :
   refonte de `_distribute_circuits_to_rcds` par famille. Type A = éclairage +
   VMC + plaque + lave-linge ; Type F = PAC + borne ; AC = reste. `requires_type_f`
   sur `Circuit`. AC réparti en round-robin (zones indépendantes, pas de DDR vide).
2. **Prises** : générales 16 A / 1,5 mm² / **8 max** ; **cuisine** dédiée 20 A /
   2,5 mm² / 6 max (`_build_kitchen_socket_circuits`, routage par catégorie).
3. **Plaque** confirmée 32 A / 6 mm² / Type A.
4. **ECS** : `_ensure_ecs` — cellier → garage si `attenant` → SDB ; attribut
   `Devis.attenant` (défaut `False` tant que Spec B absente).
5. **Pompe à chaleur** (manuelle, 32 A / 6 mm² / Type F) et **borne véhicule**
   (manuelle, 32 A / 6 mm² / Type F) — nouveaux `EquipmentType`.
6. **VMC** auto (1/logement, cellier → SDB, 16 A / 1,5 mm² / Type A) via `_ensure_vmc`.

PAC comptée plein pot dans le calibre DDR. Pastilles + palette + lignes de devis
propres pour VMC/PAC/borne. Couleurs/labels renderer pour les 4 nouveaux circuits.

## Staging — point notable
Le WIP utilisateur de `nfc_tableau.py` (ex. ECS comptée en chauffage dans le
calibre) chevauchait les mêmes fonctions que le v2 → impossible de séparer par
reconstruction. Sur décision : **WIP tableau + v2 committés ensemble** pour
`nfc_tableau.py` / `test_nfc_tableau.py` / `tableau_renderer.py`. Les fichiers de
règles/équipements/prix sont restés propres (commits dédiés).

## Tests & revue
- Suite complète **377 passed, 1 skipped** (hors slow).
- Réconciliations : comptage CircuitType (7→11), edge-case Type A (éclairage),
  enums equip (13→16), T5 n_rcds (répartition AC équilibrée).
- Revue qualité finale (multi-agents) : prête à merger, 0 critique/important ;
  nits corrigés (n_ac simplifié, svg_id `specfeed`).

## Doc
[docs/REGLES_NFC_BATIA.md](../REGLES_NFC_BATIA.md) mis à jour v2 (circuits,
familles A/F/AC, VMC/PAC/borne, ECS, prix).

## Suite
- **Spec B** : détection « garage attenant » (géométrie) à brainstormer/spécifier.
- Branche `feat/placement-chambre` toujours non clôturée (WIP renderer/scripts
  résiduel). À traiter via `superpowers:finishing-a-development-branch`.
