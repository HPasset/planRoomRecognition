# Journal — 2026-06-17

Session sur les règles métier NFC (équipements + tableau électrique), branche
`feat/placement-chambre`. Trois lots livrés en TDD, plus un document de
référence. WIP utilisateur pré-existant (tableau/renderer) préservé tout du long
via staging chirurgical (`git update-index --cacheinfo`).

## Lot 1 — Convecteurs par surface + garantie lave-linge

Spec : [docs/superpowers/specs/2026-06-16-convecteurs-lave-linge-design.md](../superpowers/specs/2026-06-16-convecteurs-lave-linge-design.md).

- **Convecteurs** : `_convector_count(nfc_cat, surface_m2)` dans
  [src/planrec/nfc_rules.py](../../src/planrec/nfc_rules.py) — `max(1, ⌈surface/20⌉)`
  si surface connue, sinon forfait **séjour = 2, chambre = 1** (avant : 1 partout).
- **Garantie lave-linge** : `_ensure_washing_machine` au niveau logement —
  au moins 1 lave-linge, sans doublon, repli **SDB → Cuisine → Garage →
  Dégagement**.
- Libellé cellier corrigé 16A → 20A (cohérence circuit réel).

## Lot 2 — Alimentations visibles (plan + devis)

Spec : [docs/superpowers/specs/2026-06-16-alim-spe-visibles-design.md](../superpowers/specs/2026-06-16-alim-spe-visibles-design.md) ·
Plan : [docs/superpowers/plans/2026-06-16-alim-spe-visibles.md](../superpowers/plans/2026-06-16-alim-spe-visibles.md).

Problème : les appareils spécialisés (Four/Plaque/LV/Lave-linge/Sèche-linge/
Cumulus) et le chauffage étaient invisibles sur le plan et hors devis
(« circuit-only ») → trou dans le devis.

- Nouvel ensemble `SPECIAL_FEED_EQUIPMENT_TYPES` (6 appareils) ; suppression de
  `CIRCUIT_ONLY_EQUIPMENT_TYPES`.
- **Pastilles** ([nfc_equipments.py](../../src/planrec/nfc_equipments.py)) : les 6
  appareils → pastille générique « Alim spé » (`SpecialFeed`) ; convecteur /
  sèche-serviettes → pastille propre. `CANVAS_HIDDEN_EQUIP_KEYS` réduit aux 6
  appareils (hors palette manuelle).
- **Cuisine** : `SOCKET` 9 → 6 (les 3 alim spé sont désormais affichées à part).
- **Facturation** ([streamlit_app.py](../../app/streamlit_app.py) +
  [pages/0_📋_Devis.py](../../app/pages/0_📋_Devis.py)) : 6 appareils agrégés en
  1 ligne « Alimentation spécialisée » par pièce (70 €) ; chauffage facturé sur
  sa propre ligne ; plus rien d'exclu. Totaux des deux chemins cohérents (498 €
  vérifié).

### Décision en cours de route (2026-06-17)
La pièce synthétique `__laundry_virtual__` (lave-linge garanti sans pièce
candidate) a été **supprimée** : un logement réduit (WC seul, chambres seules)
ne reçoit plus de lave-linge ni de ligne fantôme. Amendements ajoutés aux deux
specs.

## Lot 3 — Prises 16A / 1,5 mm² / 5 max

Retour métier : les circuits de prises doivent être en **16A**, pas 20A.

- [nfc_tableau.py](../../src/planrec/nfc_tableau.py) : `_build_socket_circuits`
  passe à `breaker_amps=16`, `cable_section_mm2=1.5` ; `SOCKET_MAX_PER_CIRCUIT`
  12 → **5** (NFC 1,5 mm²). Découpe en plus de circuits sur les grandes pièces.

## Tests
- Suite complète **357 passed, 1 skipped** (hors slow) à chaque fin de lot.
- Réconciliations : `test_nfc_equipments`, `test_nfc_rules_evolutions`,
  `test_nfc_tableau` (edge-case Type A), `test_devis_apptest` (D4 cuisine).
- Revue qualité finale (multi-agents) : prête à merger, 0 critique/important.

## Document de référence
Créé [docs/REGLES_NFC_BATIA.md](../REGLES_NFC_BATIA.md) — synthèse de toutes les
règles (équipements par pièce, chauffage, lave-linge, alim spé, circuits du
tableau, différentiels, prix) pour restitution.

## Suite
- Branche `feat/placement-chambre` non clôturée (WIP tableau/renderer toujours
  non-commité, volontairement). À traiter via
  `superpowers:finishing-a-development-branch` quand le WIP sera stabilisé.
- L'ancien spec [2026-06-02-tableau-electrique-design.md](../superpowers/specs/2026-06-02-tableau-electrique-design.md)
  mentionne encore les prises en 20A — à amender si besoin.
